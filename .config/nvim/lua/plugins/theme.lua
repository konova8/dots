return
{
  -- Gruvbox theme
  'ellisonleao/gruvbox.nvim',
  priority = 1000,
  -- config = function()
  --   vim.cmd.colorscheme 'gruvbox'
  -- end,
  config = true,
  opts = {
    contrast = "hard", -- can be "hard", "soft" or empty string
  },
}
